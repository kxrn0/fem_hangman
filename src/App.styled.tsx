import { styled } from "solid-styled-components";

const SCApp = styled("div")`
  .background {
    img {
      position: absolute;
      width: 100%;
      height: 100%;
      object-fit: cover;
    }
  }
`;

export default SCApp;
